// import { dockerizeCode2 } from '../docker-test/index2'

export default async function dockertest() {
	const path = `/Users/andrewshand/Github/frontend-virtual-ER`
	// await dockerizeCode2({
	//   gitRepository: path,
	//   destDir: '/usr/src/app',
	//   runCommand: 'yarn dev',
	//   installCommand: 'yarn',
	//   tasks: [
	//     `Dashboard page - Grey out the Discharge button with a tooltip explaining why patients cannot be discharged without a diagnosis or a discharge form`,
	//   ],
	// })
}
