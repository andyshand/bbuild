// import p from 'prompts'

export default async function aiSelect({ input: string }) {
  // What type of thing are we selecting?
  const type = 'file'

  const exampleInput = `Now add all files under the x folder`

  // stesp
  // do i need an additional query to get the files?
  // yes or no

  // if yes, what's the query
}
